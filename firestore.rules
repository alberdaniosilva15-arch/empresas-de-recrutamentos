rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /usage/{usageId} {
      allow read, write: if request.auth != null && usageId.startsWith(request.auth.uid);
    }

    match /{document=**} {
      allow read, write: if true;
    }
  }
}
